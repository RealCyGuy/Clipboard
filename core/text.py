def code(text):
    return "`" + text + "`"


# def clean_prefix(bot, ctx):
#     if ctx.prefix == "<@!" + str(bot.user.id) + "> " or ctx.prefix == "<@" + str(bot.user.id) + "> ":
#         return "@" + bot.user.name + " "
#     else:
#         return ctx.prefix
